extends ../layout

block content
  div.content-header
    div.container
      .content
        .row
          .col-lg-12
            .text-center
              if (currentUser.role === 'Admin' || currentUser.role === 'Operator')
                a(href="/muestras/new" class="btn btn-lg btn-primary btn-block mt-3") Agregar persona
                br
                a(href="/muestras/find" class="btn btn-lg btn-primary btn-block mt-3") Solicitar muestra
              br
              if (currentUser.role === 'Admin' || currentUser.role === 'Lab')
                a(href="/resultados/new" class="btn btn-lg btn-primary btn-block mt-3") Agregar resultados de muestras
              br
              if (currentUser.role === 'Admin' || currentUser.role === 'Viewer' || currentUser.role === 'Operator')
                a(href="/coincidencias/find" class="btn btn-lg btn-primary btn-block mt-3") Búsqueda de coincidencias
